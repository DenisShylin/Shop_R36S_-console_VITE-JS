export const initFeatures = () => {
  console.log("Features initialized");
  const features = document.querySelector("#features");
  if (features) {
    // Добавьте здесь логику секции features
  }
};
