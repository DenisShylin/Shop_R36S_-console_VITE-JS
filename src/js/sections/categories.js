export const initCategories = () => {
  console.log("Categories initialized");
  const categories = document.querySelector("#categories");
  if (categories) {
    // Добавьте здесь логику секции categories
  }
};
