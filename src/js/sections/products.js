export const initProducts = () => {
  console.log("Products initialized");
  const products = document.querySelector("#products");
  if (products) {
    // Добавьте здесь логику секции products
  }
};
